import { Map } from './Map';
import React from 'react';

export default {
  title: 'Map',
  component: Map,
};

const Template = (args) => <Map {...args} />;

export const Default = Template.bind({});
